import 'package:flutter/material.dart';
// Configuracion de colores para la interfaz
class AppColors {
  static const Color primaryColor = Color(0xFF0A73FF);
  static const Color secondaryColor = Color(0xFF3D8586);
  static const Color tittleColor = Color(0xFFFFFFFF);
  static const Color backgroundColor = Color(0xFF24546C);
  static const Color textColor = Color(0xFF333333);

  // Tamanio de iconos
  static const double iconSize = 35.0;
}
